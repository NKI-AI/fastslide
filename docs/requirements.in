# FastSlide Documentation Dependencies
# Base requirements - use pip_compile to generate lock files

# Core Sphinx
sphinx>=7.0.0
# furo (instead of sphinx-rtd-theme): a modern, PEP 420-clean theme. rtd-theme
# pulls in sphinxcontrib-jquery==4.1, which ships a legacy
# `sphinxcontrib/__init__.py` that shadows the PEP 420 `sphinxcontrib`
# namespace under rules_python's per-wheel site-packages layout, breaking
# imports of sphinxcontrib.{devhelp,applehelp,...}. furo has no such dep.
furo>=2024.1.29

# Breathe (Doxygen integration)
breathe>=4.35.0

# Sphinx extensions
sphinx-design>=0.5.0
sphinx-copybutton>=0.5.0
sphinx-inline-tabs>=2023.4.21
sphinxcontrib-mermaid>=0.9.0
sphinxext-opengraph>=0.9.0
sphinx-autodoc-typehints>=2.0.0

# MyST Parser (Markdown support)
myst-parser>=2.0.0

matplotlib>=3.10.0